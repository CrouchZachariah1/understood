services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: understood
      POSTGRES_PASSWORD: understood
      POSTGRES_DB: understood
    ports:
      - "5432:5432"
    volumes:
      - understood_pg:/var/lib/postgresql/data

volumes:
  understood_pg:
